#include <stdio.h>

int main() {
    // 注意：以下 emoji 字符需在支持 UTF-8 编码的环境中正确显示
    // 打印星星相关的 emoji
    printf("*\n"); 
    printf("*\n"); 
    // 打印笑脸 emoji
    printf("*\n"); 

    return 0;
}